<li>
	{{name}}

	<button class="change-eat" data-id="{{id}}" data-neweat="{{devoured}}">
		{{#if devoured}}Devour it!!{{else}}Delete a Burger{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}"></button>
</li>