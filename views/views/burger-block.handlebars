<li>
	{{name}}

	<button class="change-eating" data-id="{{id}}" data-neweating="{{eating}}">
		{{#if eating}}LET'S EAT!{{else}}OR NAH!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
