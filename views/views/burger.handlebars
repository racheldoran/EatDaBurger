<li>
	{{name}}

	<button class="change-eating" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if eating}}EATING TIME{{else}}DEVOURED!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>